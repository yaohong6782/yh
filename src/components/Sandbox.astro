---
import { Image } from "@astrojs/image/components";
import type { Tech } from "../lib";
import { Icon } from "astro-icon";
import sortingVisualiser from "../images/sorting-visualiser.png";
export interface Props {
  title: string;
  technologies: Tech[];
  demo: string;
  github: string;
}

const { title, technologies, demo, github } = Astro.props;
---

<div>
  <h3 class="pt-1 pb-3">
    <span class="bg-slate-200 border-8 rounded-lg text-xl text-slate-800"
      >{title}</span
    >
  </h3>
</div>

<div class="rounded overflow-hidden shadow-md pt-2 max-w-3xl">
  <!-- <div class="grid"> -->
  <Image src={sortingVisualiser} alt="sorting" />

  <div class="px-6 pt-4 pb-2 flex">
    {
      technologies
        .slice()
        .reverse()
        .map((tech) => {
          return (
            <div class="flex flex-col items-center group">
              <span class="inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2">
                {tech.name}
              </span>
            </div>
          );
        })
    }
    <span
      class="inline-block bg-blue-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2"
    >
      <a href={github} target="_blank">Github</a>
    </span>
    <span
      class="inline-block bg-red-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2"
      ><a href={demo} target="_blank">Demo</a></span
    >
  </div>
</div>

<!-- <div class="max-w-sm rounded overflow-hidden shadow-lg">
  <Image src={sortingVisualiser} alt="sorting" height={500} width={800} />

  <div class="px-6 py-4">
    <div class="font-bold text-xl mb-2">The Coldest Sunset</div>
    <p class="text-gray-700 text-base">
      Lorem ipsum dolor sit amet, consectetur adipisicing elit. Voluptatibus
      quia, nulla! Maiores et perferendis eaque, exercitationem praesentium
      nihil.
    </p>
  </div>
  <div class="px-6 pt-4 pb-2">
    <span
      class="inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2"
    >
      <a href={github} target="_blank">Github</a>
    </span>
    <span
      class="inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2"
      >#travel</span
    >
    <span
      class="inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2"
      >#winter</span
    >
  </div>
</div> -->
<style>
  h3 {
    /* font-weight: 500; */
    color: #6b728e;
  }
</style>
